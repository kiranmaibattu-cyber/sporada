"""Traffic-analysis inference package. Import concrete modules directly
(e.g. pipeline.analytics, pipeline.tracking); the package init stays light so
config-only imports don't pull GPU/SDK dependencies."""
